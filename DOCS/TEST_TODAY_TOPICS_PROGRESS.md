# 今日の話題 手動送信テスト 進捗メモ

**日時**: 2026-02-27（強制再起動後の実装確認）  
**目的**: 朝7時以外の時刻でも、ユーザーKENにAI秘書の「今日の話題」を送信して実装を確認する。

---

## 進捗

### 1. 方針確認
- cron は 6時・7時のみ本日のニューストピックスを配信するため、手動テスト用のCLIスクリプトを用意する。
- 対象: ユーザーKEN（ID: 6）

### 2. 作業中
- 手動送信用 CLI スクリプト `cron/send_today_topics_to_user.php` を作成（user_id 指定・--force で再送可）。
- ユーザーKEN（ID: 6）に送信を実行したところ、**メモリが 9000 超**となりスクリプトが落ちた（または環境側で強制終了）。
- **毎日の負荷について**: 朝・夜の cron は「1プロセスで全ユーザーをループ」するため、ユーザー数が増えると同様にメモリが積み上がる可能性あり。→ 対策として `DOCS/TODAY_TOPICS_CRON_MEMORY.md` を追加し、cron のループ内で `unset()` によるメモリ解放を追加済み。

---

## 結果
- （送信後に記録）
