<?php
/**
 * 金庫（メモページの「金庫」）用マスターキー
 *
 * 使い方:
 * 1. このファイルを vault.local.php にコピー
 * 2. 下の 'ここにキーを入力' を、openssl rand -hex 32 で生成した文字列に置き換え
 * 3. vault.local.php は .gitignore に追加してリポジトリに含めないこと
 *
 * 金庫の開錠は「各ユーザーのログインパスワード」で行います。
 * このキーは金庫データの暗号化用で、ユーザーには見せません。
 */
define('VAULT_MASTER_KEY', 'ここにキーを入力');
