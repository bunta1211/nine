---
description: 本番反映は main マージ＋push で自動。EC2 への直接アップロードは行わない。
alwaysApply: true
---

# 本番へのファイル反映ルール

## 運用方針（必ず守る）

- **実装ファイルを EC2 に送る作業は行わない。** 反映は **コミット → main マージ → bunta/01 と main を同期 → main と bunta/01 を push** により本番／GitHub に行う（マージでファイルを上げる運用）。
- **scp によるサーバーへのファイル送信は実行しない。** GitHub への push および main マージ後の自動デプロイに任せる。

## 通常時

- main にマージして push すると **GitHub Actions で自動デプロイ**されます。手動のファイル送信は不要です。
- **手動で反映したい場合**（緊急時など）: 手順は `DOCS/CI_CD_SETUP.md` を参照する。

## 参照ドキュメント

- **自動デプロイ・手動反映**: `DOCS/CI_CD_SETUP.md`
